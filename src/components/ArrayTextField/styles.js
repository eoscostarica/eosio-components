const Styles = (theme) => ({
  root: {
    display: "flex",
    flexDirection: "column",
  },
  chip: {
    marginTop: theme.spacing(1),
    marginRight: theme.spacing(1),
  },
  btn: {
    cursor: "pointer",
  },
});

export default Styles;
