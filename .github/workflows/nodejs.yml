name: Publish stories if changed
on:
  push:
    branches:
      - master
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout 🛎️
      uses: actions/checkout@v2
      with:
        persist-credentials: false

    - name: Install, Build, & Deploy 🚀
      run: |
        npm install 
        npm run deploy-storybook -- --ci --host-token-env-variable= ${{ secrets.ACCESS_TOKEN }}
 